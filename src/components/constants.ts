export enum EVENTS {
  BUTTON_CLICKED = 'button-event:clicked'
}

export enum SIZES {
  MEDIUM = 'md',
  LARGE = 'lg',
  EXTRA_LARGE = 'xl',
  TWO_EXTRA_LARGE = '2xl'
}

export enum VARIANTS {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  TERTIARY = 'tertiary',
  LINK_COLOR = 'link_color',
  LINK_GRAY = 'link_gray',
  DESTRUCTIVE = 'descructive'
}

export enum ICON_PLACEMENT {
  LEFT = 'left',
  RIGHT = 'right',
  BOTH = 'both'
}
